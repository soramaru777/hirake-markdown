<Window x:Class="Hirake.EditorPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="エディタを選ぶ"
        Width="560"
        SizeToContent="Height"
        MinHeight="240"
        MaxHeight="640"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="HeaderText"
                   Grid.Row="0"
                   Margin="0,0,0,10"
                   TextWrapping="Wrap"
                   Text="Hirake が見つけたエディタ:" />

        <!-- 候補が 1 件でも選ばせる。1 件だけ見つかったものが
             意図したエディタとは限らない（勝手に決めない）。 -->
        <ListBox x:Name="CandidateList"
                 Grid.Row="1"
                 MaxHeight="380"
                 MouseDoubleClick="CandidateList_MouseDoubleClick"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="4,6">
                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding ExePath}"
                                   FontSize="11"
                                   Opacity="0.75"
                                   TextTrimming="CharacterEllipsis" />
                        <TextBlock Text="{Binding Detail}"
                                   FontSize="11"
                                   Opacity="0.6"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="一覧に無い…（exe を選ぶ）"
                    Click="Browse_Click"
                    Padding="10,4"
                    Margin="0,0,8,0" />
            <Button x:Name="OkButton"
                    Content="このエディタを使う"
                    Click="Ok_Click"
                    IsDefault="True"
                    Padding="10,4"
                    Margin="0,0,8,0" />
            <Button Content="キャンセル"
                    Click="Cancel_Click"
                    IsCancel="True"
                    Padding="10,4" />
        </StackPanel>
    </Grid>
</Window>
